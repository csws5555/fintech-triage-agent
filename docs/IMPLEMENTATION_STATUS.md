# Implementation Status

Status vocabulary:

- `COMPLETE_AND_VERIFIED`
- `IMPLEMENTED_NOT_VERIFIED`
- `PARTIAL`
- `NOT_STARTED`
- `BLOCKED`

## Current phase

Phase 2 — Safe Local RAG and Triage Orchestration.

The repository implementation is complete through Step 25. The next incomplete
roadmap step is Step 26, validated buffered streaming.

Roadmap source:

`personal project documentation/phase 2 steps.md`

## Roadmap status

| Step | Status | Repository evidence |
| --- | --- | --- |
| 0 — Protect Phase 1 baseline | `COMPLETE_AND_VERIFIED` | Protected model exists; checksum manifest and verifier pass for five artifacts. |
| 1 — Phase 2 directories | `COMPLETE_AND_VERIFIED` | Backend packages, scripts, tests, and test-data directories exist; ignore rules cover generated stores and runtime output. |
| 2 — Install and verify Ollama | `COMPLETE_AND_VERIFIED` | Prior live checks confirmed `llama3.2:3b`, `nomic-embed-text`, exact generation response, and a 768-dimensional embedding. Ollama was not started or rechecked during the repository-preparation task. |
| 3 — Phase 2 dependencies | `COMPLETE_AND_VERIFIED` | Required packages are pinned in `backend/requirements.txt`; imports and `pip check` passed in the preceding pre-Step 13 verification. |
| 4 — Environment configuration | `COMPLETE_AND_VERIFIED` | `.env.example` contains all required settings; the ignored `.env` was previously confirmed to match without exposing values. |
| 5 — Central configuration | `COMPLETE_AND_VERIFIED` | `rag_config.py` validates paths, numeric constraints, loopback Ollama, model allowlists, prefixes, and limits; configuration tests pass. |
| 6 — Shared triage types | `COMPLETE_AND_VERIFIED` | Pydantic contracts are implemented and exercised by classifier/router tests. |
| 7 — Reusable classifier | `COMPLETE_AND_VERIFIED` | Local-only loading, top-three output, sorting, truncation, thresholds, and malformed-output handling are tested; prior live inference reproduced the Phase 1 reference result. |
| 8 — Approved policies | `COMPLETE_AND_VERIFIED` | Four reviewed fictional policies exist and pass policy validation. |
| 9 — Policy registry | `COMPLETE_AND_VERIFIED` | Explicit supported-intent mappings and malformed-registry checks pass. |
| 10 — Deterministic risk router | `COMPLETE_AND_VERIFIED` | Direct security overrides, policy scope, delivery distinctions, negation, hypotheticals, clarification, static responses, and unsupported routes are covered by tests. |
| 11 — Deterministic templates | `COMPLETE_AND_VERIFIED` | Required safety, clarification, refusal, limitation, and escalation templates are implemented and compile; router selection of deterministic modes is tested. |
| 12 — Policy validation | `COMPLETE_AND_VERIFIED` | YAML safety, required metadata, dates, path fields, duplicate IDs, body content, safe filenames, and distinct hash meanings are tested; validation script passes. |
| 13 — Markdown-aware chunking | `COMPLETE_AND_VERIFIED` | Validated policies are split by H1–H3 with retained canonical heading context, then by configured recursive size/overlap; safe immutable metadata and content hashes are tested across all four policies. |
| 14 — Stable record IDs | `COMPLETE_AND_VERIFIED` | IDs use document slug, normalized section-path slug, and content hash; deterministic occurrence suffixes, insertion stability, uniqueness, and unsafe-input rejection are tested. |
| 15 — Embeddings and staged Chroma rebuild | `COMPLETE_AND_VERIFIED` | Shared exact-once Nomic prefixes, explicit cosine Chroma creation, safe metadata/count/retrieval checks, stopped-client gating, staged activation, rollback, and reopen verification pass unit and live temporary-store tests. |
| 16 — Ingestion manifest | `COMPLETE_AND_VERIFIED` | Strict schema-version-2 generation, deterministic serialization, local Ollama digest lookup, package/policy/build facts, staleness checks, and temporary/reopened active-store consistency pass unit and live tests; the ignored active manifest was generated by the protected rebuild. |
| 17 — Policy-aware retriever | `COMPLETE_AND_VERIFIED` | Active-manifest/digest checks, stripped-message search, mandatory approved-policy `$in` filtering, cosine-distance conversion, safe typed candidate conversion, fake-based rules, live temporary-store score ordering, and active-store retrieval all pass. |
| 18 — Filter, deduplicate, and rank | `COMPLETE_AND_VERIFIED` | Per-candidate threshold/safety filtering, deterministic ranking, ID/text/hash deduplication, adjacent-chunk overlap removal, required-policy reservation, and global context limiting pass focused, full, live temporary-store, and active-store checks. |
| 19 — Determine retrieval sufficiency | `COMPLETE_AND_VERIFIED` | The boolean sufficiency gate fails closed for empty, unsafe, disallowed, incomplete, conflicting, stale-manifest, and over-limit contexts; valid scoped retrieval passes focused, full, live temporary-store, and active-store checks. |
| 20 — Calibrate retrieval threshold | `COMPLETE_AND_VERIFIED` | A strictly validated 10/10 calibration/locked split evaluates all seven roadmap thresholds, reports router and retriever metrics separately, selects `0.50` using calibration cases only, and confirms the selection once against the locked split. |
| 21 — Create local chat model | `COMPLETE_AND_VERIFIED` | Lazy `ChatOllama` construction revalidates allowlisted loopback settings, applies every configured runtime limit, validates the provider interface, caches one client, and passes fake-based and live initialization checks. |
| 22 — Build grounding prompt | `COMPLETE_AND_VERIFIED` | Deterministic system/human messages isolate trusted rules from JSON-encoded untrusted data, expose only safe route/chunk fields, and fail closed for unsafe routes, incomplete or invalid context, duplicates, version conflicts, limits, and local paths. |
| 23 — Use structured internal output | `COMPLETE_AND_VERIFIED` | The approved local model is bound to `GeneratedSupportResponse`; generation retries once with stricter formatting and identical context, then returns only an approved fallback and stable internal failure code. Fake-based and live loopback-Ollama tests pass. |
| 24 — Create output validation | `COMPLETE_AND_VERIFIED` | Deterministic validation inspects answer text and structured flags, aggregates every applicable ordered failure code, distinguishes safety warnings/negations from unsafe requests and claims, enforces stolen-card concepts, and never returns rejected text. |
| 25 — Implement orchestration pipeline | `COMPLETE_AND_VERIFIED` | The lazy dependency-injected synchronous pipeline enforces route order, required policy scope, sufficiency, deterministic urgent handling, bounded structured generation, output validation, safe fallbacks, and typed metadata. Focused fake and real-router seam tests pass. |
| 26–35 | `NOT_STARTED` | Validated streaming, expanded evaluation, frontend integration, and logging are not implemented. |

No roadmap step is currently `PARTIAL` or `BLOCKED`.

## Presently implemented files

Core modules:

- `backend/app/ml/rag_config.py`
- `backend/app/ml/triage_types.py`
- `backend/app/ml/classifier.py`
- `backend/app/ml/policy_registry.py`
- `backend/app/ml/risk_router.py`
- `backend/app/ml/response_templates.py`
- `backend/app/ml/policy_loader.py`
- `backend/app/ml/ingest_policies.py`
- `backend/app/ml/embeddings.py`
- `backend/app/ml/retriever.py`
- `backend/app/ml/chat_model.py`
- `backend/app/ml/grounding_prompt.py`
- `backend/app/ml/structured_generation.py`
- `backend/app/ml/output_validator.py`
- `backend/app/ml/rag_pipeline.py`

Policies:

- `backend/app/data/policies/fraud_policy.md`
- `backend/app/data/policies/card_replacement.md`
- `backend/app/data/policies/card_delivery.md`
- `backend/app/data/policies/international_fees.md`

Scripts:

- `backend/scripts/calibrate_retrieval.py`
- `backend/scripts/validate_policies.py`
- `backend/scripts/verify_phase1_baseline.py`
- `backend/scripts/rebuild_vector_store.py`

Tests:

- `backend/tests/test_rag_config.py`
- `backend/tests/test_classifier.py`
- `backend/tests/test_policy_registry.py`
- `backend/tests/test_risk_router.py`
- `backend/tests/test_policy_loader.py`
- `backend/tests/test_ingest_policies.py`
- `backend/tests/test_embedding_adapter.py`
- `backend/tests/test_vector_store_integration.py`
- `backend/tests/test_retriever.py`
- `backend/tests/test_retrieval_rules.py`
- `backend/tests/test_calibrate_retrieval.py`
- `backend/tests/test_chat_model.py`
- `backend/tests/test_chat_model_integration.py`
- `backend/tests/test_grounding_prompt.py`
- `backend/tests/test_structured_generation.py`
- `backend/tests/test_structured_generation_integration.py`
- `backend/tests/test_output_validator.py`
- `backend/tests/test_rag_pipeline.py`
- `backend/tests/data/rag_evaluation_cases.json`

## Important public interfaces

- `app.ml.rag_config.RagSettings`
- `app.ml.rag_config.settings`
- `app.ml.rag_config.load_rag_settings(...)`
- `app.ml.rag_config.validate_rag_settings(...)`
- `app.ml.classifier.get_classifier()`
- `app.ml.classifier.classify_intent(...) -> ClassificationResult`
- `app.ml.policy_registry.policies_for_intents(...)`
- `app.ml.policy_loader.PolicyDocument`
- `app.ml.policy_loader.load_policy_file(...)`
- `app.ml.policy_loader.load_policy_files(...)`
- `app.ml.policy_loader.load_policy_directory(...)`
- `app.ml.policy_loader.compute_source_hash(...)`
- `app.ml.policy_loader.compute_content_hash(...)`
- `app.ml.ingest_policies.PolicyChunk`
- `app.ml.ingest_policies.PolicyChunkingError`
- `app.ml.ingest_policies.split_policy_document(...)`
- `app.ml.ingest_policies.split_policy_documents(...)`
- `app.ml.ingest_policies.build_stable_chunk_id(...)`
- `app.ml.ingest_policies.validate_policy_chunks(...)`
- `app.ml.embeddings.NomicRagEmbeddings`
- `app.ml.embeddings.build_embeddings(...)`
- `app.ml.embeddings.get_embeddings()`
- `app.ml.ingest_policies.VectorStoreBuildResult`
- `app.ml.ingest_policies.VectorStorePaths`
- `app.ml.ingest_policies.IngestionManifest`
- `app.ml.ingest_policies.build_vector_store(...)`
- `app.ml.ingest_policies.verify_vector_store(...)`
- `app.ml.ingest_policies.activate_staged_vector_store(...)`
- `app.ml.ingest_policies.rebuild_project_vector_store(...)`
- `app.ml.ingest_policies.create_ingestion_manifest(...)`
- `app.ml.ingest_policies.validate_ingestion_manifest(...)`
- `app.ml.ingest_policies.write_ingestion_manifest(...)`
- `app.ml.ingest_policies.load_ingestion_manifest(...)`
- `app.ml.ingest_policies.get_local_embedding_model_digest(...)`
- `app.ml.ingest_policies.get_required_package_versions()`
- `app.ml.retriever.PolicyRetriever`
- `app.ml.retriever.PolicyRetriever.retrieve(..., min_relevance_score=None) -> tuple[RetrievedPolicy, ...]`
- `app.ml.retriever.PolicyRetrievalError`
- `app.ml.retriever.cosine_distance_to_relevance(...)`
- `app.ml.retriever.filter_retrieval_candidates(...)`
- `app.ml.retriever.retrieval_is_sufficient(...)`
- `app.ml.retriever.PolicyRetriever.retrieval_is_sufficient(...)`
- `app.ml.retriever.normalized_token_overlap_ratio(...)`
- `app.ml.retriever.validate_retrieval_manifest(...)`
- `app.ml.retriever.get_retriever()`
- `app.ml.chat_model.ChatModelInitializationError`
- `app.ml.chat_model.build_chat_model(...)`
- `app.ml.chat_model.get_chat_model()`
- `app.ml.grounding_prompt.GroundingPromptError`
- `app.ml.grounding_prompt.SYSTEM_GROUNDING_RULES`
- `app.ml.grounding_prompt.build_grounding_prompt(...) -> tuple[SystemMessage, HumanMessage]`
- `app.ml.structured_generation.StructuredGenerationResult`
- `app.ml.structured_generation.generate_structured_response(...) -> StructuredGenerationResult`
- `app.ml.structured_generation.STRUCTURED_GENERATION_INPUT_INVALID`
- `app.ml.structured_generation.STRUCTURED_GENERATION_UNAVAILABLE`
- `app.ml.structured_generation.STRUCTURED_GENERATION_FAILED`
- `app.ml.output_validator.OutputValidator`
- `app.ml.output_validator.output_validator`
- `app.ml.output_validator.INVALID_VALIDATION_INPUT`
- `app.ml.output_validator.EMPTY_OUTPUT`
- `app.ml.output_validator.RESPONSE_TOO_LONG`
- `app.ml.output_validator.SENSITIVE_DATA_REQUEST`
- `app.ml.output_validator.UNVERIFIED_COMPLETED_ACTION`
- `app.ml.output_validator.UNSUPPORTED_GUARANTEE`
- `app.ml.output_validator.MISSING_REQUIRED_SAFETY_CONTENT`
- `app.ml.output_validator.INTERNAL_INFORMATION_LEAK`
- `app.ml.output_validator.INSUFFICIENT_POLICY`
- `app.ml.rag_pipeline.FintechRagPipeline`
- `app.ml.rag_pipeline.rag_pipeline`
- Stable failure/fallback reason constants exported by
  `app.ml.rag_pipeline`
- `app.ml.risk_router.RiskRouter.route_message(...) -> TriageDecision`
- `app.ml.risk_router.risk_router`
- Pydantic contracts in `app.ml.triage_types`
- Deterministic functions exported by `app.ml.response_templates`, including
  negated-security, compromised-card, and unrecognized-transaction responses

Step 26 must build on these interfaces or deliberately document and test any
required compatibility change.

## Latest baseline commands actually run

Working directory: `backend/`

| Command | Status | Exact result |
| --- | --- | --- |
| `python -m compileall app scripts tests` | `COMPLETE_AND_VERIFIED` | Exit code 0; Step 25 source and all application, script, and test directories compiled. |
| `python -m pytest -q tests/test_rag_pipeline.py` | `COMPLETE_AND_VERIFIED` | `57 passed in 9.22s`; exit code 0. |
| `python -m pytest -q -m "not integration"` | `COMPLETE_AND_VERIFIED` | `384 passed, 3 deselected in 15.68s`; exit code 0. |
| `python scripts/validate_policies.py` | `COMPLETE_AND_VERIFIED` | Four policy files reported `PASS`; `4 approved policy files validated.`; exit code 0. |
| `python scripts/verify_phase1_baseline.py` | `COMPLETE_AND_VERIFIED` | Five model artifacts reported `PASS`; `5 Phase 1 model artifacts match the protected baseline.`; exit code 0. |
| `python -m pip check` | `COMPLETE_AND_VERIFIED` | `No broken requirements found.`; exit code 0. |
| `python scripts/calibrate_retrieval.py` | `COMPLETE_AND_VERIFIED` | Exit code 0; selected `0.50` on ten calibration cases and reported once on ten locked evaluation cases. Both router splits were 100%; supported retrieval hit, Recall@4, and required-family coverage were 100%, with zero false fallback and false-positive policy rate. |
| `python -m pytest -q tests/test_structured_generation_integration.py -m integration` | `COMPLETE_AND_VERIFIED` | `1 passed in 21.86s`; exit code 0. The configured loopback `llama3.2:3b` returned the required typed schema without fallback. |
| `python -m pytest -q -m integration` | `COMPLETE_AND_VERIFIED` | `3 passed, 384 deselected, 3 warnings in 26.98s`; exit code 0. Existing initialization, temporary-Chroma, and structured-generation regressions passed. The warnings were Chroma's legacy embedding-function configuration deprecation warning. |
| Initialization-only `get_chat_model()` smoke command | `COMPLETE_AND_VERIFIED` | Exit code 0; the approved configured `llama3.2:3b` model initialized with provider validation enabled. No generation prompt was sent. |
| Threshold consistency check | `COMPLETE_AND_VERIFIED` | Exit code 0; `.env.example` and the ignored local runtime environment each contain exactly one calibrated threshold entry. |

## Live-service dependencies

- DistilBERT inference uses the protected local model and does not require a
  network service.
- Step 15 live verification passed against the running loopback-only Ollama
  service with `nomic-embed-text`.
- The lazy Step 21 chat adapter initialized the installed local
  `llama3.2:3b` model through the approved loopback endpoint without sending a
  generation prompt.
- Step 22 prompt construction is pure and did not contact Ollama, Chroma, or
  the protected classifier.
- Step 23 bound the configured loopback `llama3.2:3b` model to
  `GeneratedSupportResponse` and completed a real structured generation
  without fallback. It did not access Chroma or the protected classifier.
- Step 24 validation is pure and did not contact Ollama, Chroma, or the
  protected classifier.
- Step 25 unit tests injected fake retrieval/model/validation components and
  used the real deterministic router at service-free seams. Default pipeline
  construction and deterministic branches do not resolve Ollama or Chroma.
- Chroma build, staged activation, reopen, metadata, count, cosine, and
  retrieval checks passed in a pytest temporary directory.
- Step 17's real Nomic/Chroma smoke check proved a semantically similar
  card-delivery query scored above an unrelated fraud query inside the same
  mandatory policy scope.
- The cached runtime retriever validated the configured active manifest and
  current local Nomic digest, then returned four accepted, unique,
  context-limited `card_delivery` chunks without rebuilding or swapping the
  active store.
- Step 19's bound sufficiency check passed for those four active-store chunks
  against a matching router decision and the constructor-validated manifest.
- Step 20 queried the configured active store read-only at all seven approved
  thresholds and selected `0.50`; no ingestion-related setting changed, so no
  rebuild was performed.
- Ollama was already running and was not started by the implementation.
- After the user supplied the required stopped-client confirmation,
  `python scripts/rebuild_vector_store.py --confirm-clients-stopped` rebuilt
  and reopened the project active store and manifest successfully with 67
  chunks and 768-dimensional embeddings.
- The active ignored schema-version-2 manifest records the real local
  embedding-model digest, four policy source hashes/versions, four installed
  package versions, exact prefixes/strategies/settings, and distinct source
  document and chunk counts.

## Known compatibility risks

- Installed `chromadb==1.5.9` accepts the explicit cosine configuration and
  reports cosine after reopen. It emits a legacy embedding-function
  configuration deprecation warning even though precomputed embeddings are
  supplied; this does not affect the verified result but should be reviewed
  when dependencies are upgraded.
- Windows directory moves must be treated as staged and rollback-capable, not
  universally atomic.
- The Nomic document and query prefixes must be applied exactly once.
- The calibrated `0.50` threshold preserved supported-case retrieval quality,
  but deliberately forced unsupported probes still accepted context at every
  tested threshold. The deterministic router must continue preventing
  unsupported requests from reaching retrieval.
- The Step 20 dataset has 20 cases for threshold selection and locked
  verification. Step 29 still requires the broader 40–50-case evaluation set.
- Step 24 checks explicit roadmap language patterns and structured flags.
  Regex validation is conservative rather than full semantic understanding;
  Step 29's expanded adversarial evaluation should add observed paraphrases.
- Step 25's complete branch orchestration is fake-verified, but the broader
  real-query end-to-end pipeline suite remains scheduled for Step 31.
- Callers that need required-policy diversity must pass
  `required_policy_ids`; the argument is optional only to preserve Step 17
  compatibility. The future orchestration pipeline must pass the router's
  required scope.
- Current approved policies produce 67 chunks at the configured 700/100
  size/overlap. This is derived output, not a hard-coded manifest value.
- Stable IDs do not depend on `chunk_index`; changing policy content, chunk
  settings, heading behavior, normalization, or the ID algorithm will require
  a future Chroma rebuild.
- The runtime ingestion manifest is stored inside the ignored Chroma directory
  and must be regenerated through the protected staged rebuild when policies,
  chunking, stable IDs, embedding configuration/digest/dimensions, collection
  metric/name, or relevant package versions change.
- The working tree contains extensive uncommitted Phase 2 work. Preserve it and
  do not create a commit without explicit user instruction.

## Next incomplete step

Step 26 — Add validated buffered streaming. Create or continue an execution
plan from:

`personal project documentation/phase 2 steps.md`
